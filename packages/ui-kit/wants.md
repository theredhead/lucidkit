Data & Visualization

Sparkline — tiny inline chart for embedding in table cells or KPI cards
Heatmap / calendar heatmap — GitHub-style contribution grid or data density view
Stat card / KPI tile — number + trend arrow + sparkline in a compact card (dashboard staple)
Layout & Containers

Masonry grid — Pinterest-style variable-height tile layout
Dock panel — IDE-like dockable/undockable panel system with drag-to-dock zones
Infinite scroll container — virtualised scrolling with datasource integration
Input & Selection

Transfer list — dual-pane list with move-left/move-right (user → role assignment)
Rating — star/heart/custom icon rating input
Tag input — free-text tag entry with autocomplete (like your chip but as an input field)
Mention input — @user style mention-aware text input
Code editor — syntax-highlighted code input (lightweight, no Monaco dependency)
Feedback & Overlay

Skeleton loader — animated placeholder shimmer while content loads
Empty state — illustration + message + CTA for zero-data views
Spotlight / tour — step-by-step feature walkthrough overlay highlighting DOM elements
Notification centre — stacked persistent notifications (beyond transient toasts)
Navigation & Structure

Segmented control — iOS-style toggle bar (alternative to tabs for 2–5 options)
Mega menu — full-width dropdown navigation panel with columns
Floating action button (FAB) — fixed-position action button with speed-dial expansion
Specialised / Fun

Signature pad — freehand drawing canvas for e-signatures
Diff viewer — side-by-side or inline text diff (useful alongside rich-text editor)
Sortable list — drag-and-drop reorderable list (simpler than full kanban)
Countdown / timer — configurable countdown display (event pages, auctions)
