import { Canvas, Meta } from "@storybook/addon-docs/blocks";
import * as TableViewStories from "./table-view.stories";

<Meta title="@theredhead/UI Kit/Table View/JSONPlaceholder Posts Source" />

# JSONPlaceholder Posts Source

This page demonstrates the split-source pattern for a table story: markup,
TypeScript, and styles are rendered inside a themed `ui-source-tabs` block
instead of the built-in source renderer.

<Canvas of={TableViewStories.JsonPlaceholderPosts} />

## Implementation

<Canvas of={TableViewStories.JsonPlaceholderPostsSourceTabs} />
